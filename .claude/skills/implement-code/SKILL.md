---
name: implement-code
description: 当用户明确要基于 `docs/<功能名>/` 文档集开始实施代码，或输入 `/implement-code ...` 时使用。适用于需要按阶段门禁推进实现、并在阻断时输出实施总结的功能开发任务。
---

# implement-code

---

> ## ⚠️ 强制最终步骤——每次调用完成前必须执行，禁止省略
>
> 无论代码实施是否完成（正常完成或任意阶段阻断），在生成 `06_实施总结.md` 后都必须**按顺序**完成以下四步，才算本次调用交付完成。
> **被阻断时同样必须执行，不得以"还没完成"为由跳过。**
>
> **步骤 A：动态检查清单最终完成门禁**
> 正常完成路径中，必须先读取 `docs/<功能名>/05_动态检查清单.md`，确认全部应完成检查项已完成并已回写最终状态：
> - P0 检查项必须全部为 `✅`，且备注包含阶段 6 验证证据或日期
> - "检查总结"的总体进度、待完成项和结论必须同步更新
> - 不得存在仍为 `⏳`、`🏃`、`❌` 或未说明原因的 `⚠️` 的应完成项
> - 若清单未全量完成或证据缺失，视为门禁异常，必须停止正常完成流程，生成 / 更新阻断总结并发送 wework-bot 门禁异常通知
>
> **步骤 B：回写功能文档状态**
> 读取 `.claude/skills/implement-code/rules/artifact-contracts.md`，按"功能文档状态回写契约"更新同一功能目录下相关文件：
> - `01_需求文档.md`、`02_需求任务.md`、`03_设计文档.md`、`04_使用文档.md`、`07_项目报告.md`：新增或更新 `## 实施状态` 小节
> - `05_动态检查清单.md`：更新状态列、备注列和"检查总结"
> - `06_实施总结.md`：记录状态回写结果
>
> **步骤 C：执行 import-docs 文档同步**
> 读取 `.claude/skills/import-docs/SKILL.md`，执行 `docs` 标准导入：
> ```bash
> node .claude/skills/import-docs/scripts/import-docs.js --dir docs --exts md
> ```
> - `docs` 不存在时跳过，并记录为 `☁️ 文档同步：docs 不存在，跳过导入`
> - 失败不阻断交付，但必须记录失败文件数和错误摘要
>
> **步骤 D：发送 wework-bot 完成通知**
> 读取 `.claude/skills/wework-bot/SKILL.md`，选择对应模板发送完成通知，并把步骤 C 的真实同步结果写入 `☁️ 文档同步` 行：
> - 正常完成 → 使用 "implement-code 完成（成功）" 模板（🚀✅）
> - 任意阶段阻断 → 使用 "implement-code 阻断" 模板（⛔🛠️）
> - 门禁未执行、被跳过、证据缺失或降级未记录 → 使用 "门禁异常" 模板（⛔）
> - 每条信息独占一行，使用 `━━━` 分隔线，数字填写实际值（禁止发含 `<占位符>` 的消息），并包含模型、工具、最后更新时间（精确到秒）
>
> **⛔ 完成以上四步后才算本次 implement-code 调用结束。**

---

## 定位

`implement-code` 是 YiWeb 的代码实施主编排器。

它只负责 4 件事：

1. 判断是否满足进入实施的前提
2. 串起 8 个阶段的推进顺序
3. 在每个阶段设置进入 / 退出门禁
4. 确保每次调用都在 `docs/` 下生成实施总结或兜底运行记录

阶段细则、产物模板、回写格式、报告结构不在本文件重复定义，统一下沉到 `rules/`。

## 何时使用

- 已有 `docs/<功能名>/` 文档集
- 用户明确要求开始实施代码
- 需要按阶段门禁推进，而不是直接“边写边试”

## 何时不使用

- 还没有 `02_需求任务.md`、`03_设计文档.md`、`05_动态检查清单.md`
- 用户仍在做需求澄清、设计讨论或文档生成
- 只是做零碎代码修补，不需要完整实施流程

## 全局原则

1. **文档驱动**：所有实施决策都必须能回溯到文档或代码来源。
2. **测试先行**：先生成原型页和测试骨架，再进入代码实施。
3. **门禁优先**：阶段 2 和阶段 6 的 P0 门禁未通过时，禁止继续推进。
4. **Mock 仅限测试**：生产代码禁止引入条件性 mock 或测试桩。
5. **全项目影响链分析**：代码变更前后都要在项目范围内核对上游依赖、反向依赖、传递依赖、导出链、注册链、测试、文档、配置和外部依赖影响。
6. **必须生成实施总结**：正常完成和中途阻断都必须落地 `06_实施总结.md`。
7. **每次调用必须落盘**：若 `{功能名}` 或文档集路径无法定位，必须写入 `docs/99_agent-runs/<YYYYMMDD-HHMMSS>_implement-code.md`，记录阻断原因和恢复建议。
8. **门禁失效即阻断**：任何应执行门禁未执行、被跳过、缺少证据、降级未记录或只口头声明通过，均视为 P0 阻断，必须推送 wework-bot 门禁异常消息。
9. **动态检查清单全完成才可结束**：正常完成前必须确认 `05_动态检查清单.md` 全部应完成项已更新为最终状态并同步"检查总结"；未完成、未回写或证据缺失时禁止结束。

## 阅读顺序

1. `./README.md`
2. `./rules/orchestration.md`
3. `./rules/artifact-contracts.md`
4. 按阶段读取对应规则文件

## 输入前提

默认输入：

- `{功能名}` -> `docs/<功能名>/`
- `{文档集路径}` -> 默认同上

若无法从用户请求或现有目录中确定 `{功能名}`，不得空跑或只口头询问；必须先生成兜底运行记录：

```text
docs/99_agent-runs/<YYYYMMDD-HHMMSS>_implement-code.md
```

最低文档要求：

| 文件 | 级别 | 用途 |
|------|------|------|
| `02_需求任务.md` | P0 | 用户故事、场景、前置条件 |
| `03_设计文档.md` | P0 | 模块、接口、实现约束 |
| `05_动态检查清单.md` | P0 | 全部待验证检查项 |
| `01_需求文档.md` | P1 | 背景与目标补充 |
| `04_使用文档.md` | P2 | UI 文案与用户提示补充 |

P0 文档缺失时：

1. 立即停止进入阶段 1
2. 先生成阻断版 `06_实施总结.md`
3. 再提示用户先运行 `generate-document`

详细规则见 `./rules/orchestration.md`。

## 八阶段总览

| 阶段 | 名称 | 目标 | 主要规则 |
|------|------|------|---------|
| 0 | 文档驱动 | 预检文档、Grounding、探针与分派 | `rules/orchestration.md` |
| 1 | 测试先行 | 生成测试原型页与 Playwright 骨架 | `rules/e2e-testing.md` + `rules/test-page.md` |
| 2 | 动态检查门禁 | 在原型页上验证全部 P0 项 | `rules/verification-gate.md` |
| 3 | 模块预检 | 环境预检与全项目影响链闭合分析 | `rules/orchestration.md` + `rules/code-implementation.md` |
| 4 | 写项目代码 | 逐模块实施、逐模块自检与验证 | `rules/code-implementation.md` |
| 5 | 代码评审 | code-review、Mock 泄漏检查、问题收口 | `rules/e2e-testing.md` + `rules/code-implementation.md` |
| 6 | 冒烟测试 | 在真实页面上验收全部 P0 项并回写状态 | `rules/verification-gate.md` |
| 7 | 过程总结 | 写入 `06_实施总结.md` 并回写功能文档状态 | `rules/process-summary.md` + `rules/artifact-contracts.md` |

## 阶段编排

### 阶段 0：文档驱动

- 目标：解析 `{功能名}`，完成文档预检、Grounding、分派与探针检查。
- 进入条件：用户明确要求开始实施，且目标功能目录可定位。
- 规则入口：`rules/orchestration.md`
- 退出门禁：P0 文档齐全，必要工具有可用方案。

### 阶段 1：测试先行

- 目标：为每个场景生成测试原型页和 Playwright 骨架。
- 进入条件：阶段 0 完成。
- 规则入口：`rules/e2e-testing.md`、`rules/test-page.md`、`rules/artifact-contracts.md`
- 退出门禁：测试产物、场景锚点、`data-testid` 覆盖率完整。

### 阶段 2：动态检查门禁

- 目标：在原型页面上用 Playwright-MCP 验证全部 P0 项。
- 进入条件：阶段 1 完成。
- 规则入口：`rules/verification-gate.md`、`rules/artifact-contracts.md`
- 退出门禁：P0 全部通过；否则按门禁规范阻断。

### 阶段 3：模块预检

- 目标：在写代码前完成环境预检和全项目影响链闭合分析，确认设计文档中的改动点、依赖方向、处置方式与真实代码一致。
- 进入条件：阶段 2 通过。
- 规则入口：`rules/orchestration.md`、`rules/code-implementation.md`
- 退出门禁：预检清单无阻断项。

### 阶段 4：写项目代码

- 目标：按设计文档顺序逐模块实施并立即验证。
- 进入条件：阶段 3 完成。
- 规则入口：`rules/code-implementation.md`
- 退出门禁：模块实现完成，逐模块验证结果已记录。

### 阶段 5：代码评审

- 目标：在冒烟测试前清掉所有 P0 代码问题。
- 进入条件：阶段 4 完成。
- 规则入口：`rules/e2e-testing.md`、`rules/code-implementation.md`
- 退出门禁：无 P0 问题。

### 阶段 6：冒烟测试

- 目标：在真实功能页面上验收全部 P0 项。
- 进入条件：阶段 5 完成。
- 规则入口：`rules/verification-gate.md`、`rules/artifact-contracts.md`
- 退出门禁：全部 P0 通过，且 `05_动态检查清单.md` 已回写最终状态；否则按门禁规范阻断。

### 阶段 7：过程总结

- 目标：生成完整或阻断版 `06_实施总结.md`，并更新同一功能目录内其他文档的实施状态信息。
- 进入条件：阶段 6 通过，或任意前置阶段阻断。
- 规则入口：`rules/process-summary.md`、`rules/artifact-contracts.md`
- 退出门禁：总结文件写入成功，动态检查清单最终完成门禁通过，且功能文档状态回写完成并在总结中记录。

### 阶段 8：文档同步 + 完成通知（每次必须执行，不得省略）

- 目标：通知团队并同步最新文档到 YiAi。
- 进入条件：阶段 7 完成（无论正常完成还是阻断）。
- 执行步骤：
  1. **动态检查清单最终确认**：正常完成时必须确认 `05_动态检查清单.md` 全部应完成项已完成、P0 全部 `✅`、"检查总结"已更新，并在 `06_实施总结.md` 记录最终确认结果；不满足时按门禁异常阻断。
  2. **状态回写确认**：确认阶段 7 已按 `artifact-contracts.md §功能文档状态回写契约` 更新 `01/02/03/04/05/07` 的实施状态，并在 `06_实施总结.md` 记录回写结果。
  3. **import-docs**：读取 `import-docs` 技能，执行 `docs` 标准导入（见 `import-docs/SKILL.md §docs 标准导入`）；目录不存在时记录跳过；失败不阻断交付但须保留失败文件数和错误摘要。
  4. **wework-bot**：读取 `wework-bot` 技能，**严格按照** `wework-bot/SKILL.md §生动总结格式规范` 中的对应模板发送完成通知，并把 import-docs 的真实结果写入 `☁️ 文档同步` 行：
     - 正常完成 → 使用 "implement-code 完成（成功）" 模板（🚀✅）
     - 任意阶段阻断 → 使用 "implement-code 阻断" 模板（⛔🛠️）
     - 门禁失效 → 使用 "门禁异常" 模板，写明缺失证据、门禁阶段和恢复动作
     - 每条信息独占一行，使用 `━━━` 分隔线，数字填写实际结果，并包含 `🤖 模型`、`🧰 工具`、`🕒 最后更新`
- 退出门禁：动态检查清单最终确认、状态回写、文档同步和通知发送均完成；同步或通知失败时必须记录原因、脱敏路由、模型、工具和秒级时间后才可结束。

## 停止条件

以下情况必须停止当前推进：

- `{功能名}` 或文档集路径无法定位
- P0 文档缺失
- 必要 MCP 不可用且无降级方案
- 阶段 2 达到修复上限仍未通过
- 阶段 6 达到修复上限仍未通过
- 任意阶段门禁未执行、被跳过、缺少证据、降级未记录或只口头声明通过
- 所有模块阻断且无法继续

停止时必须按顺序执行（**禁止跳过任何一步**）：

1. 记录阻断原因
2. 汇总已产生产物
3. 若功能目录可定位，生成 `docs/<功能名>/06_实施总结.md`
4. 若功能目录不可定位，生成 `docs/99_agent-runs/<YYYYMMDD-HHMMSS>_implement-code.md`
5. 若功能目录可定位，按 `artifact-contracts.md §功能文档状态回写契约` 回写阻断状态到 `01/02/03/04/05/07`，并在 `06_实施总结.md` 记录回写结果
6. **执行 import-docs 文档同步**（先于通知执行，获取真实数字）：
   ```bash
   node .claude/skills/import-docs/scripts/import-docs.js --dir docs --exts md
   ```
   `docs` 不存在时跳过，记录"目录不存在"。
7. **⚠️ 必须发送 wework-bot 阻断 / 门禁异常通知**（无论何种阻断原因，无论阶段多早，**一律发送**）：
   读取 `.claude/skills/wework-bot/SKILL.md`，使用 "implement-code 阻断" 或 "门禁异常" 模板发送通知，包含阻断原因、门禁证据、P0 统计、上一步的真实同步结果、模型、工具和最后更新时间（精确到秒）。
   > ⛔ **此步骤为阻断流程的最后一步，不得省略。省略即违反 skill 契约。**

## 支持文件结构

```text
.claude/skills/implement-code/
├── SKILL.md
├── README.md
└── rules/
    ├── orchestration.md
    ├── artifact-contracts.md
    ├── e2e-testing.md
    ├── test-page.md
    ├── verification-gate.md
    ├── code-implementation.md
    └── process-summary.md
```

## 相关技能与代理

| 名称 | 调用阶段 | 用途 |
|------|---------|------|
| `find-skills` | 阶段 0 | 声明所需 skill |
| `find-agents` | 阶段 0 | 分派代理 |
| `e2e-testing` | 阶段 1 | 生成测试方案与骨架 |
| `test-page-builder` | 阶段 1 | 构建测试原型页 |
| `verification-loop` | 阶段 3 / 5 | 静态预检与测试执行 |
| `code-review` | 阶段 5 | 真实代码审查 |
| `code-reviewer` | 阶段 5 | 架构一致性审查 |
| `architect` | 阶段 0 | 架构不确定时咨询 |
| `security-reviewer` | 阶段 0 | 安全相关场景 |
| `docs-lookup` | 阶段 0 | 文档歧义定位 |
| `search-first` | 阶段 4 | 外部依赖选型 |
| `impl-reporter` | 阶段 7 | 生成实施总结 |
| `wework-bot` | 阶段 0 / 2 / 4 / 6 / 7 / **8（必须）** | 阶段通知、门禁失败、门禁失效、阻断和完成预警；**阶段 8 必须按 `wework-bot/SKILL.md §生动总结格式规范` 发送完成总结**：成功用 🚀✅ 模板，阻断用 ⛔🛠️ 模板，门禁失效用 ⛔ 门禁异常模板，每行一条信息，数字为实际值，并包含模型、工具和秒级最后更新时间 |
| `import-docs` | **阶段 8（必须）** | 每次完成后执行 `docs` 标准导入（见 `import-docs/SKILL.md §docs 标准导入`）；目录不存在时在通知的 `☁️ 文档同步` 行说明；失败不阻断但须注明 |

## 最后规则

`implement-code` 不是“自由开发模式”。
它是一套有阶段门禁的实施流程。任何跳阶段、跳回写、跳总结的做法都视为违反 skill 契约。
