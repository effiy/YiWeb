# mermaid 图工具栏 - 验证检查清单

> **文档版本**: v1.0 | **最后更新**: 2026-04-23 | **维护者**: YiAi 团队
>
> **关联文档**: [需求文档](./需求文档.md) | [需求任务](./需求任务.md) | [设计文档](./设计文档.md) | [使用文档](./使用文档.md)
>

[通用检查](#通用检查) | [主要操作场景验证](#主要操作场景验证) | [功能实现检查](#功能实现检查) | [代码质量检查](#代码质量检查) | [测试检查](#测试检查) | [检查总结](#检查总结)

---

## 通用检查

| 检查项 | 优先级 | 状态 | 备注 |
|--------|--------|------|------|
| 文档标题格式正确 | P0 | ⏳ | |
| 所有关联文档存在且链接有效 | P0 | ⏳ | |
| 代码仓库中相关文件已创建/更新 | P0 | ⏳ | 需验证 4 个插件文件已修改 |
| 项目可正常构建 | P0 | ⏳ | |

---

## 主要操作场景验证

> 来源：[需求任务 - 主要操作场景](./需求任务.md#主要操作场景)（3 个场景）

---

### 场景：鼠标悬停显示工具栏

**关联需求任务场景**：[场景一 - 鼠标悬停显示工具栏](./需求任务.md#场景一悬停显示工具栏)

**关联设计文档实现**：[场景实现 - 鼠标悬停显示工具栏](./设计文档.md#场景实现鼠标悬停显示工具栏)

**验证工具建议**（由 find-skills 映射，均为 `.cursor/skills/` 下真实存在的技能）：
- 🏃 `e2e-testing` — UI / 用户流程场景，适合用 Playwright 模拟 mouseenter 事件验证工具栏显示
- 📋 `code-review` — 验证 ToolbarPlugin 实现符合 YiWeb Plugin 架构规范（来源：`cdn/mermaid/plugins/ToolbarPlugin.js`）

#### 前置条件验证

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 页面中包含至少一个已渲染的 mermaid 图表 | P0 | ⏳ | 检查 DOM 中存在 `.mermaid-diagram-container[data-mermaid-rendered="true"]` | 来源：需求任务#前置条件 |
| `ToolbarPlugin`、`FullscreenPlugin`、`DownloadPlugin` 均已注册到 `MermaidRenderer` | P0 | ⏳ | 检查 `cdn/mermaid/index.js` 中 `createMermaidRendererWithPlugins` 函数注册了三个插件 | 来源：设计文档#模块划分 |
| `MermaidPlugin` 使用 `createMermaidRendererWithPlugins()` 而非 `createMermaidRenderer()` | P0 | ⏳ | 检查 `cdn/markdown/plugins/MermaidPlugin.js:39` 行的导入和调用 | 来源：设计文档#修复方案 |

#### 操作步骤验证

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 将鼠标移入 `.mermaid-diagram-wrapper` 区域 | P0 | ⏳ | Playwright：`await page.hover('.mermaid-diagram-wrapper')`，验证 `.mermaid-toolbar` 的 `opacity` 变为 `1` | 来源：需求任务#操作步骤 |
| 工具栏在右上角显示，包含全屏和下载两个按钮 | P0 | ⏳ | 验证 `.mermaid-toolbar` 的 CSS `top: 8px; right: 8px`，且包含两个 `button` 子元素 | 来源：设计文档#ToolbarPlugin实现 |
| 鼠标移出图表区域后工具栏隐藏 | P1 | ⏳ | Playwright：触发 `mouseleave` 后验证 `.mermaid-toolbar` 的 `opacity` 变为 `0` | 来源：需求任务#操作步骤 |

#### 预期结果验证

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| `.mermaid-toolbar` DOM 存在于 `.mermaid-diagram-wrapper` 内部 | P0 | ⏳ | 检查 `wrapper.querySelector('.mermaid-toolbar')` 返回非空 | 来源：需求任务#预期结果 |
| 工具栏绝对定位在右上角 | P0 | ⏳ | 验证 `getComputedStyle(toolbar).position === 'absolute'` 且 `top/right` 为正确值 | 来源：设计文档#关键代码说明 |
| 按钮具有可识别的图标和 aria-label | P1 | ⏳ | 检查按钮元素有 `aria-label` 属性（如 "全屏查看"、"下载 PNG"） | 来源：需求任务#验证关注点 |

#### 验证关注点检查

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| `.mermaid-diagram-wrapper` 的 `position` 为 `relative`（非 `static`） | P0 | ⏳ | 验证 `getComputedStyle(wrapper).position !== 'static'`（ToolbarPlugin 会强制设置） | 来源：设计文档#关键代码说明 |
| 多个 mermaid 图并存时，每个图各自有独立工具栏 | P1 | ⏳ | 页面含 2+ 图表时，验证每个 wrapper 内均有 `.mermaid-toolbar` | 来源：需求任务#验证关注点 |

---

### 场景：点击全屏按钮查看图表

**关联需求任务场景**：[场景一 - 点击全屏按钮查看图表](./需求任务.md#场景一悬停显示工具栏)

**关联设计文档实现**：[场景实现 - 点击全屏按钮查看图表](./设计文档.md#场景实现点击全屏按钮查看图表)

**验证工具建议**（由 find-skills 映射）：
- 🏃 `e2e-testing` — UI 全屏模态框的创建/关闭是典型用户流程场景，适合 Playwright E2E 验证
- 📋 `code-review` — 验证 FullscreenPlugin 的 ESC 监听清理和 DOM 移除逻辑（来源：`cdn/mermaid/plugins/FullscreenPlugin.js`）

#### 前置条件验证

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 页面 mermaid 图表已渲染，SVG 内容存在于 `.mermaid-diagram-container` 内 | P0 | ⏳ | 检查 `diagram.querySelector('svg')` 返回非空 | 来源：需求任务#前置条件 |
| 工具栏已注入，全屏按钮存在 | P0 | ⏳ | 检查 `wrapper.querySelector('.mermaid-toolbar button[aria-label="全屏查看"]')` 返回非空 | 来源：需求任务#前置条件 |

#### 操作步骤验证

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 用户点击全屏按钮 | P0 | ⏳ | Playwright：`await page.click('.mermaid-toolbar button[aria-label="全屏查看"]')`，验证 `.mermaid-fullscreen-overlay` 出现在 `document.body` | 来源：需求任务#操作步骤 |
| 全屏模态框覆盖全视口 | P0 | ⏳ | 验证 `getComputedStyle(overlay).position === 'fixed'` 且 `inset` 为 `0`（或 top/left/right/bottom 均为 0） | 来源：设计文档#关键代码说明 |
| 全屏模态框内包含克隆的 SVG | P0 | ⏳ | 验证 `overlay.querySelector('svg')` 返回非空，且内容与原 diagram 的 SVG 结构一致 | 来源：需求任务#操作步骤 |

#### 预期结果验证

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 全屏模态框出现，图表内容完整可见，右上角有关闭按钮 | P0 | ⏳ | Playwright：截图验证图表可见；验证关闭按钮存在 | 来源：需求任务#预期结果 |
| 按 ESC 键关闭模态框 | P0 | ⏳ | Playwright：`await page.keyboard.press('Escape')`，验证 `.mermaid-fullscreen-overlay` 从 DOM 中移除 | 来源：需求任务#预期结果 |
| 点击关闭按钮关闭模态框 | P0 | ⏳ | Playwright：点击关闭按钮，验证 overlay 移除 | 来源：需求任务#预期结果 |
| 点击遮罩背景关闭模态框 | P1 | ⏳ | Playwright：点击 overlay 的边缘区域，验证 overlay 移除 | 来源：需求任务#预期结果 |

#### 验证关注点检查

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 关闭模态框后 ESC keydown 监听已解绑（无内存泄漏） | P1 | ⏳ | 代码审查：验证 `close()` 函数内调用了 `document.removeEventListener('keydown', escHandler)` | 来源：设计文档#关键代码说明 |
| 关闭模态框后 `.mermaid-fullscreen-overlay` 不再存在于 DOM | P0 | ⏳ | Playwright：关闭后验证 `document.querySelector('.mermaid-fullscreen-overlay') === null` | 来源：需求任务#验证关注点 |
| SVG `max-width: 90vw; max-height: 90vh` 自适应视口 | P1 | ⏳ | 验证克隆 SVG 的内联 style 包含 max-width 和 max-height 限制 | 来源：设计文档#关键代码说明 |

---

### 场景：点击下载按钮保存 PNG

**关联需求任务场景**：[场景二 - 点击下载按钮保存 PNG](./需求任务.md#场景二下载-png-图片)

**关联设计文档实现**：[场景实现 - 点击下载按钮保存 PNG](./设计文档.md#场景实现点击下载按钮保存-png)

**验证工具建议**（由 find-skills 映射）：
- 🏃 `e2e-testing` — 下载按钮点击触发文件下载是 UI 流程场景，Playwright 可拦截 download 事件验证
- 📋 `verification-loop` — 验证 Canvas API 和 Blob URL 在浏览器构建环境中可用，适合集成验证

#### 前置条件验证

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| mermaid 图表已渲染完成，SVG 内容存在于容器内 | P0 | ⏳ | 检查 `diagram.querySelector('svg')` 返回非空 | 来源：需求任务#前置条件 |
| 工具栏已注入，下载按钮存在 | P0 | ⏳ | 检查 `wrapper.querySelector('.mermaid-toolbar button[aria-label="下载 PNG"]')` 返回非空 | 来源：需求任务#前置条件 |
| 浏览器支持 Canvas API 和 Blob URL | P0 | ⏳ | 验证 `typeof Canvas !== 'undefined'` 及 `typeof URL.createObjectURL === 'function'`（来源：设计文档#依赖关系） | 来源：设计文档#依赖关系 |

#### 操作步骤验证

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 点击下载按钮触发 PNG 文件下载流程 | P0 | ⏳ | Playwright：`page.waitForEvent('download')` 后点击下载按钮，验证 download 事件触发 | 来源：需求任务#操作步骤 |
| SVG 序列化为字符串（XMLSerializer） | P0 | ⏳ | 代码审查：验证 `DownloadPlugin.js` 中使用 `new XMLSerializer().serializeToString(svg)` | 来源：设计文档#关键代码说明 |
| Canvas 绘制 SVG 并导出 PNG Blob | P0 | ⏳ | 代码审查：验证使用 `canvas.toBlob(callback, 'image/png')` 导出 | 来源：设计文档#关键代码说明 |

#### 预期结果验证

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 下载文件名格式符合 `mermaid-diagram-{timestamp}.png` | P0 | ⏳ | Playwright：`const download = await downloadEvent; expect(download.suggestedFilename()).toMatch(/mermaid-diagram-\d+\.png/)` | 来源：需求任务#预期结果 |
| 下载文件格式为 PNG | P0 | ⏳ | Playwright：验证下载文件名以 `.png` 结尾；或验证 Blob type 为 `image/png` | 来源：需求任务#预期结果 |
| 下载后 Blob URL 已释放（`URL.revokeObjectURL`） | P1 | ⏳ | 代码审查：验证 `DownloadPlugin.js` 中在 `a.click()` 后调用了 `URL.revokeObjectURL(a.href)` | 来源：设计文档#关键代码说明 |

#### 验证关注点检查

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| Canvas 设置了背景色（避免透明背景导致 PNG 看不清图表） | P1 | ⏳ | 代码审查：验证 `ctx.fillStyle = '#0f172a'` 在 `drawImage` 前调用（来源：设计文档#关键代码说明） | 来源：设计文档#验证要点 |
| Canvas 宽高与 SVG 一致（或使用 devicePixelRatio 放大） | P1 | ⏳ | 代码审查：验证 `canvas.width` 和 `canvas.height` 基于 `img.width * dpr` 计算 | 来源：设计文档#关键代码说明 |
| SVG Blob URL 在 Image 加载后释放（避免内存泄漏） | P1 | ⏳ | 代码审查：验证 `img.onload` 回调中调用了 `URL.revokeObjectURL(url)` | 来源：设计文档#关键代码说明 |

---

## 功能实现检查

> 来源：[设计文档 - 修复内容](./设计文档.md#修复内容)

### 核心功能检查

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| `ToolbarPlugin.afterRender` 已实现（非空占位） | P0 | ⏳ | 检查 `cdn/mermaid/plugins/ToolbarPlugin.js` 的 `afterRender` 方法体不为空 | 来源：设计文档#修复方案 |
| `FullscreenPlugin.afterRender` 已实现（非空占位） | P0 | ⏳ | 检查 `cdn/mermaid/plugins/FullscreenPlugin.js` 的 `afterRender` 方法体不为空 | 来源：设计文档#修复方案 |
| `DownloadPlugin.afterRender` 已实现（非空占位） | P0 | ⏳ | 检查 `cdn/mermaid/plugins/DownloadPlugin.js` 的 `afterRender` 方法体不为空 | 来源：设计文档#修复方案 |
| `MermaidPlugin` 已改用 `createMermaidRendererWithPlugins()` | P0 | ⏳ | 检查 `cdn/markdown/plugins/MermaidPlugin.js:38-39` 行是否使用 `createMermaidRendererWithPlugins` | 来源：设计文档#修复方案 |

### 边界情况检查

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| diagram 没有 parentElement 时 ToolbarPlugin 不报错 | P1 | ⏳ | 代码审查：验证 `afterRender` 首行有 `if (!wrapper) return;` 防御检查 | 来源：设计文档#核心流程图 |
| FullscreenPlugin/DownloadPlugin 找不到 .mermaid-toolbar 时不报错 | P1 | ⏳ | 代码审查：验证两个插件在找不到 toolbar 时有 early return | 来源：设计文档#核心流程图 |
| diagram 容器内没有 SVG 时点击全屏/下载不报错 | P1 | ⏳ | 代码审查：验证两个插件的点击处理函数首行有 `if (!svg) return;` | 来源：设计文档#关键代码说明 |
| 渲染中（loading 状态）的图表点击按钮 | P2 | ⏳ | 手动测试：在图表渲染中途点击按钮，验证无 JS 错误 | 来源：需求文档#验收标准 |

### 错误处理检查

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| Canvas toBlob 失败时有错误提示 | P2 | ⏳ | 代码审查：验证 `canvas.toBlob` 回调中有 null 检查 | 来源：设计文档#测试考虑 |
| 多次点击全屏按钮不创建多个 overlay | P1 | ⏳ | 快速连续点击全屏按钮，验证 DOM 中只有一个 `.mermaid-fullscreen-overlay` | 来源：设计文档#测试考虑 |

---

## 代码质量检查

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 代码风格符合项目规范 | P1 | ⏳ | `code-review` 技能：验证 ES6+ 语法、4空格缩进、const/let、单引号、箭头函数（来源：`rules/编码规范.md`） | |
| 函数/变量命名清晰 | P1 | ⏳ | 人工复核：函数名使用规范前缀（`create`/`open`/`download`/`handle`），布尔变量用 `is/has` 前缀 | |
| 无明显性能问题 | P2 | ⏳ | 浏览器 DevTools Performance 面板：悬停/点击操作不产生超过 100ms 的主线程阻塞 | |
| 无安全隐患 | P0 | ⏳ | 人工复核：不使用 `eval`、`innerHTML` 拼接用户数据（SVG 克隆使用 `cloneNode`，不进行字符串拼接） | |

---

## 测试检查

| 检查项 | 优先级 | 状态 | 验证方法 | 备注 |
|--------|--------|------|----------|------|
| 单元测试覆盖核心功能 | P1 | ⏳ | 检查是否有针对 `ToolbarPlugin`、`FullscreenPlugin`、`DownloadPlugin` 的单元测试文件 | |
| E2E 测试覆盖主要操作场景 | P0 | ⏳ | `e2e-testing` 技能 + Playwright：运行 mermaid 图工具栏 E2E 测试套件，覆盖悬停显示、全屏、下载三个场景 | |
| 所有 P0 测试通过 | P0 | ⏳ | 运行测试套件，无 P0 失败项 | |
| 测试报告完整 | P1 | ⏳ | 检查测试输出包含通过/失败统计和各场景验证结果 | |

---

## 检查总结

### 总体进度

| 类别 | 总项数 | 已完成 | 通过率 |
|------|--------|--------|--------|
| 通用检查（P0） | 4 | 0 | 0% |
| 场景验证（P0） | 17 | 0 | 0% |
| 场景验证（P1） | 11 | 0 | 0% |
| 功能实现检查（P0） | 4 | 0 | 0% |
| 功能实现检查（P1/P2） | 6 | 0 | 0% |
| 代码质量检查 | 4 | 0 | 0% |
| 测试检查 | 4 | 0 | 0% |
| **总计** | **50** | **0** | **0%** |

### 待完成项

- [ ] 实现 `cdn/mermaid/plugins/ToolbarPlugin.js` 的 `afterRender` 逻辑
- [ ] 实现 `cdn/mermaid/plugins/FullscreenPlugin.js` 的 `afterRender` 逻辑
- [ ] 实现 `cdn/mermaid/plugins/DownloadPlugin.js` 的 `afterRender` 逻辑
- [ ] 修改 `cdn/markdown/plugins/MermaidPlugin.js` 改用 `createMermaidRendererWithPlugins()`
- [ ] 编写 E2E 测试用例（Playwright）覆盖三个主要操作场景
- [ ] 执行所有 P0 测试并确认通过

### 结论

⏳ 检查未开始
