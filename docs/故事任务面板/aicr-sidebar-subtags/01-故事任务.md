# 故事任务：aicr-sidebar-subtags

> \| v2 \| 2026-05-10 \| — \| — \| — \| — \|
> **证据标准**: A=已验证(附路径) · B=可推导(附规则) · C=未验证(标注 `> 待补充`) · D=禁止(视为幻觉)
> **技术评审**: 详见 [03-前端技术评审.md](./03-前端技术评审.md) 和 [04-测试用例评审.md](./04-测试用例评审.md)

---

## §1 Story

**角色场景：** AICR 用户在浏览文件树时，需要在侧边栏内用二级目录标签筛选文件，同时期望侧边栏在页面滚动时保持可见（吸顶）。

**价值：**
- 侧边栏始终显示二级目录标签，用户可直接点击子目录名筛选文件
- 侧边栏吸顶确保文件树和标签筛选始终可操作，不随代码区滚动而消失

**范围边界：**
- 纯前端 UI/UX 改进，无后端接口变更
- 涉及 `fileTree/index.html`、`fileTree/index.css`、`fileTreeComputed.js`、`.aicr-sidebar` CSS 定位
- 复用现有 `AicrHeader` 标签组件的事件和状态（`selectedSessionTags`、`tagFilter*`）
- Sidebar 标签仅展示二级目录名，不展示一级目录名；一级目录筛选仍由 Header 负责
- 二级标签与 Header 一级标签联动：选中一级标签后，Sidebar 仅显示该一级目录下的二级标签；未选时不显示二级标签

**依赖：** 现有 `AicrHeader` 标签系统（拖拽排序、展开/折叠）已就绪

---

## §1.1 User Operations

**主操作流：**
1. 用户在 AICR 页面看到侧边栏（文件树视图）
2. 侧边栏顶部显示二级目录标签列表
3. 用户可直接点击二级标签筛选文件树
4. 一级目录标签仍通过 Header 选择，与 Sidebar 二级标签共用 `selectedTags`
5. 清除所有标签选中后，文件树恢复无筛选状态
6. 用户滚动代码区时，侧边栏保持吸顶可见

**UI 布局：**

```
┌──────────────────────────────────────────────────┐
│ AICR Header                                      │
│ [文件树] [代码]     [🏷 src] [cdn] [docs]  ← 一级标签 │
├────────────────┬─────────────────────────────────┤
│ Sidebar        │ Code Area                       │
│ (sticky 吸顶)  │                                  │
│ ┌────────────┐ │  // ... 代码内容 ...            │
│ │ 🏷 二级标签 │ │  ← 始终显示所有二级目录标签      │
│ │ [components]│ │    [components] [utils] [hooks]  │
│ │ [utils]    │ │                                  │
│ ├────────────┤ │                                  │
│ │ 📁 文件树  │ │                                  │
│ │ ├─ src/    │ │  ← 点击二级标签后文件树筛选      │
│ │ │ ├─ comp… │ │                                  │
│ │ │ ├─ utils │ │                                  │
│ │ │ └─ hooks │ │                                  │
│ └────────────┘ │                                  │
│ ◄─ 独立滚动 ─► │ ◄─ 页面滚动，sidebar 保持可见 ─► │
└─────────────────────────────────────────────────┘
```

> 窄屏（≤950px）：sidebar 变为顶部横条，不吸顶。

---

## §2 Requirements

| # | 功能点 | 输入 | 输出 | 错误行为 |
|---|-------|------|------|---------|
| F1 | 侧边栏始终显示二级目录标签 | `fileTree` 数据 | 渲染所有二级目录名作为标签 | 无二级目录时隐藏标签区域 |
| F2 | 标签点击筛选、展开/折叠 | 用户点击标签按钮 | emit `tag-select`（二级标签加入 selectedTags），`tag-filter-expand` | — |
| F3 | 侧边栏吸顶 | 页面滚动 | `.aicr-sidebar` 使用 `position: sticky; top: 0` | — |
| F4 | 二级标签筛选文件树 | 用户点击二级标签 | `sortedTree` 按二级标签过滤子目录 | 无匹配时显示空状态 |

**业务规则：**
- 侧边栏标签仅显示二级目录名（所有一级目录下的子目录集合，去重排序）
- 二级标签与一级标签共用 `selectedTags` 数组，`sortedTree` 区分一级/二级分别筛选
- 标签列表最大默认显示 8 个，超出显示展开按钮
- 侧边栏吸顶在 `.aicr-main` 容器内生效，sidebar 自身可独立滚动

---

## §3 Design

### 技术设计

**模块 1 — fileTree 标签 UI（`fileTree/index.html` + `index.css`）**

在 `file-tree-search-wrapper` 下方添加 `sidebar-tags-section` 区域。标签列表包含：标签按钮列表（含「没有标签」按钮 + 展开/折叠），不显示搜索框。

**模块 2 — fileTree 标签层级联动（`fileTreeComputed.js`）**

`allTags` 计算属性始终返回所有二级目录名：
- 遍历 `fileTree` 所有一级目录，收集其子目录名
- 去重、排序，应用用户自定义拖拽顺序

`tagCounts` 同步适配：统计每个二级目录名在所有一级目录下的累计文件数。

`sortedTree` 区分一级/二级标签筛选（逻辑不变）：
- 一级标签筛选：过滤一级目录（Header 选择生效）
- 二级标签筛选：过滤子目录（只保留名称匹配的子文件夹，文件保留）

**模块 3 — 侧边栏吸顶（`styles/index.css` + `aicrSidebar/index.css`）**

`.aicr-sidebar` 添加 `position: sticky; top: 0; align-self: flex-start; max-height: 100vh; overflow-y: auto;`。

`.aicr-main` 保持 `overflow: auto` 或 `overflow: visible` 以确保 sticky 生效。

### 安全约束
- 无后端接口变更，无 XSS/SQL 注入风险
- 标签内容来自文件树数据（用户可控文件系统），需确保标签渲染使用 textContent 而非 innerHTML

---

## §4 Tasks

| # | 任务 | 文件 | 交付物 |
|---|------|------|--------|
| T1 | fileTree 标签 UI 结构 | `fileTree/index.html` | 移除搜索框，保留 sidebar-tags-section |
| T2 | fileTree 标签样式 | `fileTree/index.css` | 移除搜索框样式，保留标签列表样式 |
| T3 | fileTree 标签仅显示二级目录 | `fileTreeComputed.js` | allTags / tagCounts 改为始终返回二级目录 |
| T4 | 侧边栏吸顶样式 | `styles/index.css` / `aicrSidebar/index.css` | sticky 定位 + 独立滚动 |
| T5 | 集成验证 | `index.html` | 浏览器验证二级标签显示 + 吸顶 |

---

## §5 Acceptance Criteria

| # | 标准 | 测试方法 |
|---|------|---------|
| AC1 | Sidebar 始终显示所有二级目录标签 | 视觉检查 |
| AC2 | 点击 Sidebar 二级标签可筛选文件树 | 点击后观察文件树变化 |
| AC3 | 清除所有标签后，文件树恢复无筛选状态 | 点击清除后观察 |
| AC4 | 页面滚动时，Sidebar 保持吸顶可见 | 滚动代码区观察 sidebar 位置 |
| AC5 | Sidebar 标签支持展开/折叠 | 交互测试 |

---

## §6 .claude 改进清单

- `rui-claude` skill 在清理旧故事时需保留执行记忆，避免完全丢失上下文

---

## §7 系统架构演进任务

- 无

---

## §L 自我改进循环

> 待 rui code 完成后由 self-improve-loop 自动追加
