# 04-测试用例评审 — YiWeb-accessibility-polish

## 测试策略

人工验证 + grep 扫描。

## 测试用例

### TC-01: Reduced Motion 全覆盖

| 项 | 内容 |
|----|------|
| 目的 | 所有含 transition/animation 的文件含 `prefers-reduced-motion` |
| 步骤 | `find` + `grep` 扫描 |
| 预期 | 无遗漏 |

### TC-02: 硬编码色值清理

| 项 | 内容 |
|----|------|
| 目的 | 无 `rgba(234,179,8,1)` / `#fca5a5` / `rgba(0,0,0,0.15)` 等 |
| 步骤 | `grep` 扫描 |
| 预期 | 无命中 |

### TC-03: Legacy 变量清理

| 项 | 内容 |
|----|------|
| 目的 | 无 `var(--shadow-sm)` / `var(--transition-fast)` / `var(--shadow-primary)` |
| 步骤 | `grep` 扫描 |
| 预期 | 无命中 |

## 门禁标准

- TC-01 为 P0
- TC-02、TC-03 为 P1
