# 08 — 测试用例报告：统一主题色与基础组件

## 测试执行摘要

### Phase 1：主题色统一

| 用例 | 结果 | 备注 |
|------|------|------|
| TC-01 | 通过 | theme.css 规范层完整，遗留段已标记 @deprecated |
| TC-02 | 通过 | 遗留 CSS 变量 0 处 |
| TC-03 | 通过 | 非图标直接硬编码 0 处 |
| TC-04 | 通过 | JS 直接硬编码 0 处 |
| TC-05 | 未执行 | 需人工浏览器验证（暗色模式） |
| TC-06 | 未执行 | 需人工浏览器验证（高对比度） |
| TC-07 | 未执行 | 需人工浏览器验证（减少动效） |
| TC-08 | 通过 | `--pet-chat-main-color` 已收敛至 `--yi-primary` |
| TC-09 | 通过 | 未定义变量扫描 0 处 |
| TC-10 | 通过 | 默认暗色主题：`:root` surface/text/border/shadow 为暗色值 |
| TC-11 | 通过 | 亮色模式覆盖：`@media (prefers-color-scheme: light)` 恢复全部亮色值 |
| TC-12 | 通过 | 对比度验证：所有文本/背景组合 >= 4.5 |

### Phase 2：基础组件统一

| 用例 | 结果 | 备注 |
|------|------|------|
| IN-01 | 通过 | 高度 44px，Token 正确 |
| IN-02 | 通过 | 高度 36px |
| IN-03 | 通过 | 高度 52px |
| IN-04 | 通过 | `var(--yi-shadow-focus)` |
| IN-05 | 通过 | `--yi-danger` + `--yi-danger-subtle` |
| IN-06 | 通过 | opacity 0.5 |
| IN-07 | 通过 | `update:modelValue` 已绑定 |
| IN-08 | 通过 | `--yi-text-muted` |
| TA-01 | 通过 | 最小高度 80px |
| TA-02 | 通过 | 默认 vertical |
| BT-01 | 通过 | validator 已包含 accent |
| BT-02 | 通过 | `.btn-block` 已定义 |
| BT-03 | 通过 | min-height 52px |
| BT-04 | 通过 | md=44px 与 YiInput/YiSelect 一致 |
| IB-01 | 通过 | 36px |
| IB-02 | 通过 | 52px |
| IB-03 | 通过 | primary-subtle + primary |
| IB-04 | 通过 | md=44px |
| TG-01 | 通过 | validator 已包含 accent |
| TG-02 | 通过 | 24px |
| TG-03 | 通过 | 36px |
| TG-04 | N/A | 项目中无 `size="small"` 使用 |
| SL-01 | 通过 | 触发器 36px，option 32px |
| SL-02 | 通过 | 触发器 52px，option 48px |
| SL-03 | 通过 | md=44px |
| SL-04 | 通过 | `.select` 类正确匹配 |

### 视图级验证

| 用例 | 结果 | 备注 |
|------|------|------|
| VW-01 | 通过 | focus 使用 `var(--yi-border-focus)` + `var(--yi-shadow-focus)` |
| VW-02 | 通过 | 同上 |
| VW-03 | 通过 | 收敛至 yi-* Token |
| VW-04 | 通过 | `var(--yi-shadow-focus)` |

### 主题级验证

| 用例 | 结果 | 备注 |
|------|------|------|
| TH-01 | 通过 | Token 推导：所有组件使用 `--yi-*` Token |
| TH-02 | 通过 | Token 推导：`theme.css` 媒体查询已覆盖 |
| TH-03 | 通过 | Token 推导：组件 CSS 含 `prefers-contrast` |
| TH-04 | 通过 | Token 推导：组件 CSS 含 `prefers-reduced-motion` |

## 详细结果

### TC-01：主题根文件完整性

**执行**：`cat cdn/styles/theme.css | grep -c 'yi-'`
**结果**：157 个 `yi-*` token 引用点，覆盖 Primary/Semantic/Surfaces/Borders/Text/Shadows/Radius/Animation/Z-Index
**遗留段标记**：`/* Legacy Variable Map (BACKWARD COMPAT — @deprecated) */` 已添加
**状态**：通过

### TC-02：CSS 遗留变量清零

**执行**：
```bash
grep -r "var(--primary)\b\|var(--bg-primary)\b\|var(--text-primary)\b\|var(--border-primary)\b" src/views/aicr --include="*.css"
```
**结果**：0 处匹配
**状态**：通过

### TC-03：CSS 硬编码颜色清零

**执行**：
```bash
grep -rn ": #[0-9a-fA-F]\{3,6\}\|;#[0-9a-fA-F]\{3,6\}" src/views/aicr --include="*.css" | grep -v "fileTreeTreeBase.css" | grep -v "var(--"
```
**结果**：0 处直接硬编码（文件类型图标品牌色保留）
**状态**：通过

### TC-04：JS 动态样式收敛

**执行**：
```bash
grep -rn "color: #[0-9a-fA-F]\{3,6\}\|background: #[0-9a-fA-F]\{3,6\}" src/views/aicr --include="*.js" | grep -v "var(--"
```
**结果**：0 处直接硬编码
**状态**：通过

### TC-08：主题色一致性

**执行**：
```bash
grep -n "pet-chat-main-color" src/views/aicr/styles/codePage.css
```
**结果**：`--pet-chat-main-color: var(--yi-primary);`
**状态**：通过

### TC-09：token 可用性

**执行**：Node.js 脚本扫描所有 CSS 变量引用与 theme.css 定义
**结果**：未定义变量 0 处（排除 `--pet-chat-*` 业务局部变量）
**状态**：通过

### TC-10：默认暗色主题

**执行**：检查 `:root` 中 `yi-bg`/`yi-surface`/`yi-text`/`yi-border`/`yi-shadow-*` 默认值
**结果**：
- `--yi-bg: #0F172A` ✓
- `--yi-surface: #1E293B` ✓
- `--yi-text: #F8FAFC` ✓
- `--yi-border: rgba(255,255,255,0.1)` ✓
- `--yi-shadow-sm: 0 1px 2px rgba(0,0,0,0.3)` ✓
**状态**：通过

### TC-11：亮色模式覆盖

**执行**：检查 `@media (prefers-color-scheme: light)` 中是否恢复全部亮色值
**结果**：surface/border/text/subtle/shadow 共 17 个 token 已恢复亮色值
**状态**：通过

### TC-12：对比度验证

**执行**：Node.js 脚本计算关键文本/背景组合的 WCAG 对比度
**结果**：

| 组合 | 对比度 | 结果 |
|------|--------|------|
| `yi-text` on `yi-bg` | 17.06 | PASS |
| `yi-text` on `yi-surface` | 13.98 | PASS |
| `yi-text-secondary` on `yi-surface` | 9.85 | PASS |
| `yi-text-muted` on `yi-surface` | 5.71 | PASS |
| `yi-text` on `yi-surface-elevated` | 9.90 | PASS |

**状态**：通过

### 组件级验证摘要

**YiInput / YiTextarea**：8/8 通过。高度、Token、focus、error、disabled、v-model、placeholder、resize 全部验证通过。

**YiButton**：4/4 通过。accent、block、lg、对齐全部验证通过。

**YiIconButton**：4/4 通过。size sm/lg、variant primary、对齐全部验证通过。

**YiTag**：3/3 通过（1 N/A）。accent、size sm/lg 验证通过；旧类名兼容 N/A（无存量引用）。

**YiSelect**：4/4 通过。size sm/lg、对齐、CSS 类名修复全部验证通过。

## 回归验证

- [x] YiButton 原有 variant（primary/secondary/outline/ghost/danger）未受影响
- [x] YiTag 原有 variant（primary/success/warning/danger/info）未受影响
- [x] YiSelect 交互逻辑未变更
- [x] YiIconButton 默认行为未变更（className 默认值由 `icon-button` 改为空，通过 computedClass 自动注入）
- [x] 无新增硬编码颜色值
- [x] Phase 1 遗留变量修复无回退

## 未执行项说明

TC-05~07 需浏览器人工验证：
- 零构建项目无自动化浏览器测试框架
- 验证方式：打开 `index.html` → DevTools Rendering → 切换 `prefers-color-scheme` / `prefers-contrast` / `prefers-reduced-motion`
- 建议由 QA 或开发者在合并前执行

## 回归范围确认

| 区域 | 验证深度 | 风险评级 |
|------|---------|---------|
| 文件树侧边栏 | 全量替换 | 中 |
| 代码阅读区 | 全量替换 | 中 |
| 聊天面板 | 全量替换 | 中 |
| 弹窗/模态框 | 全量替换 | 高（contextModals 变更最多）|
| 搜索面板 | 抽样 | 低 |
| 拖拽条 | 已收敛 | 低 |
| 基础组件 | 全量检查 | 中 |

## 结论

静态验证全部通过（29/29 通过，1 N/A）。建议在合并前补充浏览器人工验证（约 10 分钟）。
