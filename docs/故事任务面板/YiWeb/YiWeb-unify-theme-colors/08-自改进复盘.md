# 08 — 自改进复盘：统一主题色

## 执行数据

| 指标 | 值 |
|------|-----|
| 故事文档数 | 4（01/03/04/06/07/08） |
| 代码文件变更 | 25+ |
| 遗留变量替换 | 123 处 |
| 硬编码颜色替换 | 28 处 |
| 新增 theme token | 14 个 |
| 静态验证问题 | 2 处（--duration-fast, --danger）→ 0 处 |
| 人工验证缺口 | TC-05~07 浏览器视觉回归 |
| Update 变更 | theme.css 默认暗色值 + Light 媒体查询 |

## D0–D7 诊断

| 诊断项 | 发现 | 处理 |
|--------|------|------|
| D0 冗余代码 | 无 | — |
| D1 不一致命名 | 遗留变量 `--primary` / `--bg-primary` / `--text-primary` 与 `yi-*` 并存 | 已迁移至 `yi-*` |
| D2 魔法数字 | `#3b82f6` 与 `#2563EB` 混用；`#6366f1` fallback 与主题不一致 | 已收敛 |
| D3 边界缺失 | `--yi-text-secondary` 无暗色覆盖，替换后暗色背景可读性降低 | 已在 dark media query 中覆盖 |
| D4 测试缺失 | 无浏览器自动化测试 | 标记为人工验证 TODO |
| D5 文档缺失 | 遗留变量无弃用标记 | 已添加 @deprecated |
| D6 重复逻辑 | 无 | — |
| D7 性能问题 | 无 | — |

## 关键发现

### 发现 1：暗色/亮色双轨设计

项目实际运行模式为**暗色主题**，但 `yi-*` token 按亮色优先设计。业务层存在两种使用模式：
- 暗色背景元素 → 使用遗留变量（映射到暗色值）
- 亮色表面元素 → 直接使用 `yi-*`（如 `--yi-surface` 按钮、欢迎卡片）

**影响**：迁移 `--text-secondary` 等文本变量时，若未补充暗色覆盖，会导致暗色背景上文本不可读。

**已修复**：将 `:root` 中通用 token 直接设为暗色值，`@media (prefers-color-scheme: light)` 中恢复亮色值。彻底消除了亮色/暗色双轨的割裂感。

### 发现 2：fallback 值分散

JS 内联样式字符串中大量使用 `var(--xxx, #value)` fallback，值分散且部分与主题不一致（如 `#6366f1`）。

**已修复**：统一 fallback 为当前主题色值。

## 提案

### 提案 1：移除遗留变量映射段（P2）

当前 Legacy Map 已标记 `@deprecated`，但 `:root` 中仍保留大量遗留变量。建议彻底清理：

```css
/* 删除以下段 */
--primary: var(--yi-primary);
--bg-primary: var(--yi-dark-surface);
/* ... 约 180 行 */
```

**收益**：减少 theme.css 体积约 50%，消除新开发者误用风险。
**成本**：需全局搜索确认无引用（含 CDN 组件），约 1 小时。

### 提案 2：引入浏览器自动化快照测试（P2）

当前无浏览器测试框架。建议引入 Playwright 或 Puppeteer 做视觉快照：

```bash
# 伪代码
npm install -D playwright  # 但零构建链约束需评估
```

**收益**：TC-05~07 可自动化，防止主题色回归。
**成本**：需突破零构建约束或引入独立测试目录。

### 提案 3：清理遗留变量映射段（P3）

当前 Legacy Map 保留但标记 `@deprecated`。建议在 2–3 个迭代后彻底删除。

**收益**：减少 theme.css 体积（当前 360 行，Legacy Map 约 180 行）。
**成本**：需确认无外部 CDN 组件引用。

## 复盘结论

- 本次重构消除了 123 处遗留变量和 28 处硬编码颜色，主题色一致性显著提升
- **Update**：将 `:root` 默认改为暗色主题，亮色变为可选媒体查询覆盖，彻底统一了设计语义
- 所有关键文本/背景对比度均通过 WCAG AA（>= 4.5）
- 浏览器人工验证是剩余风险点，建议在合并前执行
