# 07-测试用例报告 — YiWeb-aicr-layout-ux

## 测试策略

人工验证 + DevTools 检查清单 + 静态审查。

## 测试结果汇总

| 用例 | 状态 | 备注 |
|------|------|------|
| TC-01 布局令牌覆盖 | 通过 | 硬编码 indigo 已清除，使用 `rgba(var(--yi-primary-rgb), ...)` |
| TC-02 动画标准化 | 通过 | 过渡使用 `--yi-duration-*` / `--yi-easing-default` |
| TC-03 Reduced Motion | 通过 | layout.css / codePage.css / contextModals.css / welcomeCard.css 均含媒体查询 |
| TC-04 空状态卡片 | 通过 | 渐变发光已移除，使用 surface + border + shadow |
| TC-05 聊天气泡 | 通过 | 用户气泡使用 `var(--yi-primary)` 纯色 |
| TC-06 模态框聚焦 | 通过 | 聚焦环使用 `--yi-border-focus` 语义令牌 |

## 详细结果

### TC-01: 布局令牌验证

- **实际结果**: 通过 `grep` 扫描，4 个文件中 `rgba(79,70,229` 硬编码已全部替换为 `rgba(var(--yi-primary-rgb),`。
- **验证方式**: 源码静态扫描。
- **结论**: 通过。

### TC-02: 动画标准化

- **实际结果**: 所有 `0.3s cubic-bezier(0.4, 0, 0.2, 1)` / `0.2s ease` / `0.25s ease` 已替换为令牌值。
- **验证方式**: `grep` 扫描确认无离散时长残留。
- **结论**: 通过。

### TC-03: Reduced Motion

- **实际结果**:
  - `layout.css`: 覆盖 sidebar-expand-handle、chat-expand-handle、file-tree-toggle、sidebar-resizer、feature-cards-grid
  - `codePage.css`: 覆盖 streaming caret 动画
  - `codePage.contextModals.css`: 新增覆盖 modal-body、faq-modal-body、所有按钮 hover transform
  - `welcomeCard.css`: 覆盖 card、tag、action-btn、url
- **结论**: 通过。

### TC-04: 空状态卡片

- **实际结果**: `pet-chat-empty-card` 和 `code-view-container .empty-card` 均使用 `var(--yi-surface)` + `var(--yi-border)` + `var(--yi-shadow-lg)`，移除多重渐变、玻璃态、发光顶线、图标 after 伪元素发光。
- **结论**: 通过。

### TC-05: 聊天气泡

- **实际结果**: `.pet-chat-message.is-user .pet-chat-bubble` 使用 `background: var(--yi-primary)` 纯色。
- **结论**: 通过。

### TC-06: 模态框聚焦

- **实际结果**: `.aicr-session-context-textarea:focus` 使用 `border-color: var(--yi-border-focus)`。
- **结论**: 通过。

## 回归验证

- [x] 所有 CSS 文件括号平衡验证通过
- [x] `codePage.css` 预存语法错误已修复（重复声明块移除）
- [x] `src/views/aicr/index.html` 未改变

## 门禁结论

- P0 用例（TC-01 ~ TC-04）全部通过
- P1 用例（TC-05、TC-06）全部通过
- **Gate B 结论: 通过**
