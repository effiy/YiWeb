# YiWeb-系统架构-依赖矩阵 · 测试设计

> v1.0.0 | 2026-05-28 | deepseek-v4-pro | feat/yiweb-arch-sub-stories

> **导航**: [← 技术评审](./技术评审.md)

> [§0 基线溯源](#sec0) · [§1 测试范围](#sec1) · [§2 用例](#sec2) · [§3 Gate A 交接](#sec3)

### 主要价值

- ✅ 验证关键模块清单数量达标 — ≥ 15 个覆盖 L0–L3
- 📊 确认依赖矩阵行列完整 — N×N 格式无缺行缺列
- 🚦 验证高风险模块识别 — Top-5 含被依赖次数和下游列表
- 🔍 确认依赖方向校验 — 无反向依赖违规

<a id="sec0"></a>

## §0 基线溯源

| 基线文件 | 关键条款 | 本次适用性 | 偏差 |
|---------|---------|-----------|------|
| 故事任务.md | FP5.1–FP5.6、AC1–AC6 | 全部适用 — 测试覆盖所有 FP | 无 |
| 使用场景.md | 4 场景全覆盖 | 全部适用 — 测试覆盖所有场景的关键路径 | 无 |
| 技术评审.md | 关键模块选取规则、矩阵格式、高风险识别规则、方向校验表 | 全部适用 — 测试验证规则和格式 | 无 |
| CLAUDE.md | 分层约束 | 适用 — 依赖方向校验依据 | 无 |

<a id="sec1"></a>

## §1 测试范围

| 维度 | 覆盖 | 不覆盖（原因） |
|------|------|---------------|
| 关键模块数量 | 清单中模块总数和各层分布 | 选取规则是否最优（需人工评审） |
| 矩阵完整性 | N×N 格式、行列标签一致 | 交叉格标记的准确性（需逐项人工验证） |
| 依赖方向 | 对照分层约束的违规检查 | 间接依赖的传递性影响 |
| 高风险识别 | Top-5 清单的被依赖次数统计 | 阈值的合理性（需按项目规模调整） |

<a id="sec2"></a>

## §2 用例

### 关键模块清单

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-DEP-001 | 关键模块数量达标 | 分层结构和模块地图产出可用 | 统计关键模块清单中的模块总数 | ≥ 15 个 | P1 | FP5.1 |
| TC-DEP-002 | 关键模块覆盖四层 | 关键模块清单完成 | 逐层统计模块数量 | L0 ≥ 1 个 + L1 ≥ 3 个 + L2 ≥ 5 个 + L3 ≥ 6 个 | P1 | FP5.1 |
| TC-DEP-003 | 关键模块五大分类覆盖 | 关键模块清单完成 | 统计分类覆盖 | 通用组件 ≥ 5 + 基础设施 ≥ 5 + 服务层 ≥ 3 + 视图 ≥ 3 | P1 | FP5.1 |

### 依赖提取

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-DEP-004 | 每关键模块有依赖列表 | 关键模块入口文件可读 | 逐一检查每个关键模块是否记录了依赖列表 | 全部关键模块有依赖列表（可为空） | P1 | FP5.2 |
| TC-DEP-005 | 依赖列表含 import 路径 | 依赖列表完成 | 检查每个依赖项是否含模块名 + import 路径 | 每项含模块名和路径 | P1 | FP5.2 |
| TC-DEP-006 | 间接依赖标注存在 | 全部模块源码可搜索 | 搜索 eventBus.emit/on 调用，检查矩阵中是否标注 | 事件总线依赖在矩阵中标注为 ○ | P2 | FP5.2 |

### 依赖方向

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-DEP-007 | 无 L3→L1 违规 | 依赖列表完成 | 检查是否有基础设施模块依赖视图入口 | 0 违规 | P1 | FP5.3 |
| TC-DEP-008 | 无 L3→L2 违规 | 依赖列表完成 | 检查是否有基础设施模块依赖服务层模块 | 0 违规 | P1 | FP5.3 |
| TC-DEP-009 | 无 L2→L1 违规 | 依赖列表完成 | 检查是否有服务层模块依赖视图入口 | 0 违规 | P1 | FP5.3 |
| TC-DEP-010 | 违规清单格式正确 | 违规检查完成 | 如有违规项，检查清单是否含依赖方 + 被依赖方 + 违规方向 + 等级 | 每项四列完整 | P2 | FP5.3 |

### 矩阵完整性

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-DEP-011 | 矩阵行列一致 | 依赖矩阵已生成 | 检查行标签和列标签是否为同一模块集合 | 行标签 = 列标签 = 关键模块清单 | P1 | FP5.4 |
| TC-DEP-012 | 矩阵格式含图例 | 依赖矩阵已生成 | 检查是否含 ●（直接依赖）/ ○（间接依赖）/ -（无依赖）图例 | 三种标记均有说明 | P1 | FP5.4 |
| TC-DEP-013 | 矩阵无缺行缺列 | 依赖矩阵已生成 | 逐行逐列检查格子是否存在 | N×N 格子全部填充 | P1 | FP5.4 |

### 高风险模块

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-DEP-014 | 高风险模块清单 Top-5 | 依赖矩阵已生成 | 统计每列 ● + ○ 总数，排序取 Top-5 | Top-5 各含模块名 + 被依赖次数 + 下游列表 | P2 | FP5.5 |
| TC-DEP-015 | 被依赖次数统计准确 | 依赖矩阵已生成 | 手工抽查 3 个模块的被依赖次数 | 手工计数 = 矩阵统计数 | P2 | FP5.5 |

### 依赖关系图

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-DEP-016 | 依赖关系 mermaid 图存在 | 技术评审文档存在 | 检查是否含依赖关系图 | mermaid flowchart 含 ≥ 10 节点 + 依赖箭头 | P2 | FP5.6 |
| TC-DEP-017 | 高风险模块在图中有标注 | 依赖关系图已生成 | 检查图中的高风险模块是否有视觉区分 | 高风险模块节点有特殊标记或颜色 | P2 | FP5.6 |

<a id="sec3"></a>

## §3 Gate A 交接信号

| 检查项 | 状态 |
|--------|:---:|
| TC 覆盖全部 FP#（FP5.1–FP5.6） | ✅ |
| 六类测试全覆盖（关键模块清单/依赖提取/依赖方向/矩阵完整性/高风险模块/依赖关系图） | ✅ |
| P0 用例 ≥ 8 个 | N/A — 本故事全为 P1/P2（架构文档类） |
| P1 用例 ≥ 4 个 | ✅（11 个） |
| 每个 TC 含 Given/When/Then 等价描述（场景/前置/步骤/预期） | ✅ |
| 基线溯源表覆盖故事任务+使用场景+技术评审+CLAUDE.md | ✅ |

---

> **变更记录**：v1.0.0 — 从父故事 yiweb-arch FP5 拆分创建（2026-05-28，`/rui update`）
