# YiWeb-系统架构-安全边界 · 测试设计

> v1.0.0 | 2026-05-28 | deepseek-v4-pro | feat/yiweb-arch-sub-stories

> **导航**: [← 技术评审](./技术评审.md)

> [§0 基线溯源](#sec0) · [§1 测试范围](#sec1) · [§2 用例](#sec2) · [§3 Gate A 交接](#sec3)

### 主要价值

- ✅ 验证五类信任面防护机制存在 — 每面有对应的防护代码
- 🔒 确认接口面防护链完整 — credentials:omit → X-Token → 401拦截 → 登录弹窗
- 🧹 确认存储面无敏感数据泄露 — localStorage 仅含凭证/环境/调试
- 🛡️ 确认渲染面 SanitizePlugin 启用且排序首位

<a id="sec0"></a>

## §0 基线溯源

| 基线文件 | 关键条款 | 本次适用性 | 偏差 |
|---------|---------|-----------|------|
| 故事任务.md | FP4.1–FP4.6、AC1–AC7 | 全部适用 — 测试覆盖所有 FP | 无 |
| 使用场景.md | 4 场景全覆盖 | 全部适用 — 测试覆盖所有场景的关键路径 | 无 |
| 技术评审.md | 五类信任面详细标注、接口面防护链、认证面生命周期、渲染面安全链 | 全部适用 — 测试验证防护机制存在 | 无 |
| CLAUDE.md | 安全面定义、credentials: 'omit' 要求 | 全部适用 — 安全基线依据 | 无 |

<a id="sec1"></a>

## §1 测试范围

| 维度 | 覆盖 | 不覆盖（原因） |
|------|------|---------------|
| 输入面 | 4 类输入入口的防护机制存在性 | 参数校验的具体规则正确性（需人工审查） |
| 接口面 | 请求凭证链的 4 步机制存在性 | 后端认证逻辑（非本系统边界） |
| 存储面 | localStorage 写入调用的内容审计 | 运行时动态生成的存储键名 |
| 认证面 | 凭证生命周期闭环的机制存在性 | 凭证加密强度（取决于传输层） |
| 渲染面 | SanitizePlugin 启用状态和排序 | 清洗规则是否覆盖新型 XSS 向量 |

<a id="sec2"></a>

## §2 用例

### 输入面

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-SEC-001 | 地址栏参数校验存在 | config.js 可读 | 读取 config.js，检查 URLSearchParams 处理逻辑 | 含参数读取和校验逻辑 | P0 | FP4.1 |
| TC-SEC-002 | 文件上传预检存在 | projectZipMethods.js 可读 | 读取文件上传逻辑，检查是否含前端校验 | 含文件类型或大小检查 | P0 | FP4.1 |
| TC-SEC-003 | 文本输入清洗链路存在 | inputMethods.js 可读 | 读取输入处理逻辑，确认输入数据流转到 MarkdownRenderer | 输入 → 渲染器 → SanitizePlugin 链路存在 | P0 | FP4.1 |
| TC-SEC-004 | 面板数据加载入口存在 | story/claude 视图入口可读 | 读取数据加载逻辑，确认数据来源 | 数据来源为已知远端 | P1 | FP4.1 |

### 接口面

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-SEC-005 | credentials: 'omit' 存在 | requestHelper.js 可读 | grep `credentials` 关键字 | 含 `credentials: 'omit'` | P0 | FP4.2 |
| TC-SEC-006 | X-Token 认证头注入存在 | authUtils.js + requestHelper.js 可读 | 读取请求封装逻辑，检查是否注入认证头 | 请求头含 X-Token 设置 | P0 | FP4.2 |
| TC-SEC-007 | 401 拦截逻辑存在 | authErrorHandler.js 可读 | 读取错误处理逻辑，检查是否检查 401 | 含 401 状态码判断 | P0 | FP4.2 |
| TC-SEC-008 | 登录弹窗触发逻辑存在 | authErrorHandler.js 可读 | 读取 401 处理分支，检查是否触发登录弹窗 | 401 时调用登录弹窗 | P0 | FP4.2 |
| TC-SEC-009 | 无裸 fetch 调用绕过封装 | 源码可搜索 | grep 全项目 `fetch(` 调用 | 所有 fetch 调用经请求封装或显式设 credentials: 'omit' | P1 | FP4.2 |

### 存储面

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-SEC-010 | localStorage 写入调用审计 | 源码可搜索 | grep 全项目 `localStorage.setItem` | 列出全部调用及其存储内容 | P0 | FP4.3 |
| TC-SEC-011 | 无敏感数据存储 | 上一步结果 | 逐项审查存储内容 | 仅含凭证/环境/调试标识，无密码、无 token（除 X-Token）、无用户隐私数据 | P0 | FP4.3 |
| TC-SEC-012 | 凭证清除逻辑存在 | authErrorHandler.js 可读 | 检查 401 处理是否含 `localStorage.removeItem` | 凭证过期时清除对应存储项 | P1 | FP4.3 |

### 认证面

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-SEC-013 | 凭证读取逻辑存在 | authUtils.js 可读 | 检查是否含从 localStorage 读取凭证的逻辑 | 含 `localStorage.getItem` 调用 | P0 | FP4.4 |
| TC-SEC-014 | 凭证过期检测存在 | authErrorHandler.js 可读 | 检查是否含 401 响应检测 | 含响应状态码检查 | P0 | FP4.4 |
| TC-SEC-015 | 凭证生命周期闭环 | authUtils.js + authErrorHandler.js | 追踪存储→读取→注入→过期→清除→重认证全流程 | 6 步均有对应代码，形成闭环 | P0 | FP4.4 |

### 渲染面

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-SEC-016 | SanitizePlugin 文件存在 | cdn/markdown/plugins/ 目录存在 | 检查 SanitizePlugin.js 是否存在 | 文件存在 | P0 | FP4.5 |
| TC-SEC-017 | SanitizePlugin 在插件列表中 | PluginSystem.js 或 MarkdownRenderer.js 可读 | grep SanitizePlugin 引用 | SanitizePlugin 被导入和注册 | P0 | FP4.5 |
| TC-SEC-018 | 无直接 innerHTML 操作 | 源码可搜索 | grep 全项目 `innerHTML` 调用 | 0 结果 或全部在安全清洗插件内部 | P1 | FP4.5 |

### 全景图

| TC# | 场景 | 前置 | 步骤 | 预期 | 优先级 | 覆盖 FP# |
|-----|------|------|------|------|--------|---------|
| TC-SEC-019 | 安全边界全景图完整 | 技术评审文档存在 | 检查全景图是否含 Browser 沙箱（五面 subgraph）+ External 外部系统 | 6 个 subgraph + 信任方向箭头 | P0 | FP4.6 |
| TC-SEC-020 | 安全防护清单完整 | 技术评审文档存在 | 检查总表是否五面各 ≥ 1 条，含入口点 + 防护机制 + 关键文件 + 风险等级 | 5 行记录，四列完整 | P0 | FP4.6 |

<a id="sec3"></a>

## §3 Gate A 交接信号

| 检查项 | 状态 |
|--------|:---:|
| TC 覆盖全部 FP#（FP4.1–FP4.6） | ✅ |
| 五类测试全覆盖（输入面/接口面/存储面/认证面/渲染面）+ 全景图 | ✅ |
| P0 用例 ≥ 8 个 | ✅（14 个） |
| P1 用例 ≥ 4 个 | ✅（6 个） |
| 每个 TC 含 Given/When/Then 等价描述（场景/前置/步骤/预期） | ✅ |
| 基线溯源表覆盖故事任务+使用场景+技术评审+CLAUDE.md | ✅ |

---

> **变更记录**：v1.0.0 — 从父故事 yiweb-arch FP4 拆分创建（2026-05-28，`/rui update`）
